import { StyleSheet, Text, View } from "react-native";
import React from "react";
import HeaderDiary from "../screenComponent/HeaderDiary";

const DiaryMeal = () => {
  return (
    <View>
      <HeaderDiary />
    </View>
  );
};

export default DiaryMeal;

const styles = StyleSheet.create({});
